```markdown
# 🚀 AV messages — Telegram Bot with Mini App
 
**AV messages** — это мощный и гибкий Telegram-бот, интегрированный с собственным мини-приложением. Он решает проблему невозможности инициировать диалог с пользователями, которые не писали первыми (спам-блоки Telegram), предоставляя удобный канал обратной связи через защищённое мини-приложение.

---

## ✨ Ключевые возможности

### 👤 Для пользователей
| Функция | Описание |
|--------|----------|
| **Интуитивное мини-приложение** | Отправка сообщений, просмотр истории и получение ответов в одном интерфейсе |
| **Поддержка любых типов сообщений** | Текст, фото, видео, голосовые, документы и стикеры (через бота) |
| **Мгновенные уведомления** | При ответе администратора пользователь получает 🔔 с кнопкой для перехода в приложение |
| **Rate‑limit 10 минут** | Защита от спама и флуда |
| **Блокировка с указанием причины** | Пользователь видит причину и срок бана |

### 👑 Для администраторов
| Функция | Описание |
|--------|----------|
| **Ответы через `#ID`** | Простой формат: `#123 Ваш ответ` — ответ мгновенно уходит пользователю |
| **Управление администраторами** | Добавление / удаление администраторов через бота |
| **Полная статистика** | Количество пользователей, сообщений, банов, ответов |
| **Блокировка / разблокировка** | Гибкая система бана (навсегда или на N часов) |
| **Список всех пользователей** | Просмотр базы пользователей с количеством отправленных сообщений |

### 🛡️ Технические особенности
| Особенность | Описание |
|-------------|----------|
| **Постоянное хранение данных** | PostgreSQL сохраняет пользователей, сообщения, ответы и статистику |
| **Мини‑приложение внутри Telegram** | Работает как Web App, не требует отдельного сайта |
| **Единый процесс на Render** | Бот и API работают в одном контейнере, нет конфликта портов |
| **Кэширование списка админов** | Снижает нагрузку на базу данных |
| **Graceful shutdown** | Корректное завершение работы при получении сигналов SIGTERM/SIGINT |

---

## 🏗️ Архитектура системы

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Пользователь│────▶│   Telegram  │────▶│   Render    │
│  (Telegram)  │◀────│     API     │◀────│   Сервер    │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                                 │
                                        ┌────────┴────────┐
                                        │   PostgreSQL    │
                                        │  (Render DB)    │
                                        └─────────────────┘
```

### Компоненты
- **Telegram Bot API** — взаимодействие с пользователями и отображение мини‑приложения.
- **aiogram 3.4.1** — асинхронный фреймворк для работы с Telegram API.
- **FastAPI + aiohttp** — два HTTP-сервера, объединённые в одном процессе:
  - FastAPI (uvicorn) обслуживает статику и API для мини‑приложения.
  - aiohttp обрабатывает вебхуки и внутренние эндпоинты бота.
- **PostgreSQL + asyncpg** — основное хранилище данных.
- **Render.com** — хостинг (веб-сервис + база данных).

---

## 🧰 Технологический стек

| Категория | Технологии |
|-----------|------------|
| **Backend** | Python 3.11.11, aiogram 3.4.1, FastAPI 0.110.0, aiohttp 3.9.3, asyncpg 0.29.0 |
| **Frontend** | HTML5, CSS3, JavaScript (Telegram Web App SDK) |
| **Database** | PostgreSQL 15 |
| **Hosting** | Render (Web Service + PostgreSQL) |
| **Deploy** | Git, Render Blueprint / Manual Deploy |

---

## 📋 Требования к окружению

- Python **3.11.11** (строго, с версиями выше могут возникнуть проблемы с зависимостями)
- PostgreSQL (локально или на Render)
- Аккаунт на [Render.com](https://render.com)
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

---

## 🚀 Установка и запуск

### Локальная разработка

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/artemvershinski/mini_app_bot.git
cd mini_app_bot

# 2. Создайте виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate   # Linux/Mac
# или
venv\Scripts\activate      # Windows

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Создайте файл .env
echo "BOT_TOKEN=ваш_токен" >> .env
echo "DATABASE_URL=postgresql://user:pass@localhost:5432/messagebot" >> .env
echo "APP_URL=http://localhost:10000" >> .env

# 5. Запустите базу данных PostgreSQL (локально или через Docker)

# 6. Запустите приложение
python main.py
```

### Деплой на Render

#### Через Blueprint (рекомендуется)
1. Форкните репозиторий на GitHub.
2. На [dashboard.render.com](https://dashboard.render.com) нажмите **New +** → **Blueprint**.
3. Подключите свой репозиторий и выберите ветку.
4. Render автоматически создаст Web Service и PostgreSQL, используя `render.yaml`.
5. Добавьте в переменные окружения Web Service ваш `BOT_TOKEN`.
6. Нажмите **Apply**.

#### Вручную
1. Создайте **Web Service** и **PostgreSQL**.
2. В настройках Web Service укажите:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python main.py`
3. Добавьте переменные окружения:
   - `BOT_TOKEN`
   - `DATABASE_URL` (Internal URL из PostgreSQL)
   - `APP_URL` (URL вашего сервиса, например `https://ваш-сервис.onrender.com`)
4. Запустите деплой.

---

## ⚙️ Конфигурация (переменные окружения)

| Переменная | Описание | Пример |
|------------|----------|--------|
| `BOT_TOKEN` | Токен Telegram бота | `8597895667:AAElq0IsGIXjIVkUx8BrT1AqRhc7UfURmYs` |
| `DATABASE_URL` | Строка подключения к PostgreSQL | `postgresql://user:pass@host:5432/dbname` |
| `APP_URL` | Публичный URL мини‑приложения (используется в кнопках) | `https://ваш-сервис.onrender.com` |
| `PORT` | Порт, на котором работает сервер (Render устанавливает автоматически) | `10000` |

---

## 📱 Использование

### Для обычных пользователей
1. Напишите боту команду `/start`.
2. Нажмите кнопку **📱 Открыть приложение** — откроется мини‑приложение.
3. Во вкладке **Отправленные** вы увидите историю своих сообщений.
4. Чтобы написать новое сообщение, введите текст в поле внизу и нажмите **Отправить**.
5. Когда администратор ответит, вы получите уведомление 🔔 с кнопкой для перехода в приложение.

### Для администраторов
1. После добавления в админы, вы сможете использовать команды управления.
2. **Ответ пользователю**: в чате с ботом отправьте `#ID текст ответа`, например `#100569 Спасибо за обращение!`.
3. Команды:
   - `/stats` — статистика бота
   - `/users` — список пользователей
   - `/ban ID причина [часы]` — заблокировать пользователя
   - `/unban ID` — разблокировать
   - `/admin add ID` / `/admin remove ID` — управление админами
   - `/clear_db_1708` — полная очистка всех сообщений (только для владельца)

---

## 🌐 API эндпоинты (для мини‑приложения)

Мини‑приложение общается с сервером через следующие REST-эндпоинты:

| Метод | Путь | Описание |
|-------|------|----------|
| `POST` | `/api/auth` | Аутентификация пользователя по `initData` |
| `POST` | `/api/send` | Отправка нового сообщения |
| `GET` | `/api/messages/inbox` | Получение входящих (ответы админов) |
| `GET` | `/api/messages/sent` | Получение отправленных сообщений |

Все запросы, кроме `/api/auth`, должны содержать заголовок `X-Telegram-Init-Data` с `initData` от Telegram.

---

## 🗄️ Структура базы данных

### Таблица `users`
| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | BIGINT | PRIMARY KEY, ID пользователя Telegram |
| `username` | TEXT | @username |
| `first_name` | TEXT | Имя |
| `last_name` | TEXT | Фамилия |
| `last_message_time` | TIMESTAMP | Время последнего сообщения |
| `is_banned` | BOOLEAN | Забанен ли пользователь |
| `ban_until` | TIMESTAMP | Дата окончания бана |
| `ban_reason` | TEXT | Причина бана |
| `messages_sent` | INTEGER | Количество отправленных сообщений |
| `created_at` | TIMESTAMP | Дата регистрации |
| `updated_at` | TIMESTAMP | Дата обновления |

### Таблица `messages`
| Поле | Тип | Описание |
|------|-----|----------|
| `message_id` | INTEGER | PRIMARY KEY, уникальный ID сообщения |
| `user_id` | BIGINT | FOREIGN KEY → users(user_id) |
| `content_type` | TEXT | Тип контента (text, photo, video…) |
| `file_id` | TEXT | ID файла для медиа |
| `caption` | TEXT | Подпись к медиа |
| `text` | TEXT | Текст сообщения |
| `forwarded_at` | TIMESTAMP | Время отправки |
| `is_answered` | BOOLEAN | Отвечено ли |
| `answered_by` | BIGINT | ID администратора, ответившего |
| `answered_at` | TIMESTAMP | Время ответа |
| `answer_text` | TEXT | Текст ответа |

### Таблица `admins`
| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | BIGINT | PRIMARY KEY, ID администратора |
| `added_by` | BIGINT | Кто добавил |
| `added_at` | TIMESTAMP | Дата добавления |
| `is_active` | BOOLEAN | Активен ли |

### Таблица `stats`
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | SERIAL | PRIMARY KEY |
| `total_messages` | INTEGER | Всего сообщений |
| `successful_forwards` | INTEGER | Успешных пересылок |
| `failed_forwards` | INTEGER | Ошибок пересылки |
| `bans_issued` | INTEGER | Количество банов |
| `rate_limit_blocks` | INTEGER | Срабатываний лимита |
| `answers_sent` | INTEGER | Отправленных ответов |
| `updated_at` | TIMESTAMP | Время последнего обновления |

### Таблица `message_counter`
| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | PRIMARY KEY, всегда 1 |
| `last_message_id` | INTEGER | Последний использованный ID сообщения |

---

## 📜 Полный список команд бота

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/start` | Все | Начало работы, приветствие и кнопка открытия Mini App |
| `/app` | Все | Открыть мини‑приложение |
| `/help` | Все | Справка по командам |
| `/stats` | Админ | Статистика работы бота |
| `/users` | Админ | Список пользователей |
| `/ban ID причина [часы]` | Админ | Блокировка пользователя |
| `/unban ID` | Админ | Разблокировка |
| `/admin` | Админ | Управление администраторами |
| `/clear_db_1708` | Владелец | Очистка всех сообщений из БД |

---

## 🤝 Участие в разработке

Мы приветствуем любые предложения и улучшения!  
Чтобы внести вклад:

1. Форкните репозиторий.
2. Создайте ветку для своей функции: `git checkout -b feature/amazing-feature`
3. Закоммитьте изменения: `git commit -m 'Add some amazing feature'`
4. Запушьте ветку: `git push origin feature/amazing-feature`
5. Откройте Pull Request.

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**.  
Подробности в файле [LICENSE](LICENSE).

---

**AV messages** — современное, надёжное и красивое решение для организации обратной связи через Telegram.  
Сделано с ❤️ и заботой о пользователях.
